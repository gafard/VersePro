# Backend VersePro v2 - Requirements (Python 3.14 compatible)

# FastAPI + WebSocket
fastapi>=0.109.0
uvicorn[standard]>=0.27.0
websockets>=12.0

# Audio & Transcription
deepgram-sdk>=3.1.0
pyaudio>=0.2.14
numpy>=1.24.0
vosk>=0.3.44
huggingface-hub>=0.20.0
# Binding Python OFFICIEL de transcribe.cpp — ctypes pur, généré depuis
# l'en-tête, avec vérification de l'ABI au chargement. Il ne contient PAS la
# bibliothèque native : celle-ci est livrée dans bin/ (voir versepro-backend.spec).
# Source : tmp/transcribe_src/bindings/python
transcribe-cpp>=0.2.0
# Embeddings multilingues ONNX pour le retrieval biblique local.
fastembed>=0.8.0

# HTTP client (Agent IA : OpenRouter / Gemini / Ollama)
httpx>=0.27.0

# ProPresenter Integration
aiohttp>=3.9.0

# Rendu des cartouches NDI (sortie NDI)
pillow>=10.0.0

# Database
aiosqlite>=0.19.0
keyring>=25.0.0,<26.0.0
# Autorites TLS : indispensable une fois l'application figee (PyInstaller),
# sinon les telechargements de modeles echouent en CERTIFICATE_VERIFY_FAILED.
certifi>=2024.2.2

# Utils
python-dotenv>=1.0.0
pyyaml>=6.0.1
pydantic>=2.10.0
pydantic-settings>=2.6.0
python-osc>=1.8.3
ndi-python>=5.1.0

# Logging
loguru>=0.7.2

# Tests
pytest>=8.0.0
