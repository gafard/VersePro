# Backend VersePro v2 - Requirements (Python 3.14 compatible)

# FastAPI + WebSocket
fastapi>=0.109.0
uvicorn[standard]>=0.27.0
websockets>=12.0

# Audio & Transcription
deepgram-sdk>=3.1.0
# L'audio arrive du navigateur (PCM 16 kHz via WebSocket) : PyAudio n'est pas
# requis par le backend et n'est donc pas installé. Cela évite le blocage
# Windows/Python 3.14 où aucune roue PyAudio officielle n'est publiée.
numpy>=1.24.0
vosk>=0.3.44
huggingface-hub>=0.20.0
# Moteur ASR local Nemotron. Deux paquets qui vont ensemble et DOIVENT rester
# à la même version : le binding est du ctypes pur généré depuis l'en-tête, et
# il vérifie l'ABI de la native au chargement — une version décalée devient une
# ImportError, pas une corruption mémoire. Les roues « native » embarquent la
# bibliothèque compilée pour chaque plateforme : rien à compiler côté VersePro.
transcribe-cpp==0.1.3
transcribe-cpp-native==0.1.3
# Embeddings multilingues ONNX pour le retrieval biblique local.
fastembed>=0.8.0

# HTTP client (Agent IA : OpenRouter / Gemini / Ollama)
httpx>=0.27.0

# ProPresenter Integration
aiohttp>=3.9.0

# Rendu des cartouches NDI (sortie NDI)
pillow>=10.0.0

# Database
aiosqlite>=0.19.0
keyring>=25.0.0,<26.0.0
# Autorites TLS : indispensable une fois l'application figee (PyInstaller),
# sinon les telechargements de modeles echouent en CERTIFICATE_VERIFY_FAILED.
certifi>=2024.2.2

# Utils
python-dotenv>=1.0.0
pyyaml>=6.0.1
pydantic>=2.10.0
pydantic-settings>=2.6.0
python-osc>=1.8.3
ndi-python>=5.1.0

# Logging
loguru>=0.7.2

# Tests
pytest>=8.0.0
